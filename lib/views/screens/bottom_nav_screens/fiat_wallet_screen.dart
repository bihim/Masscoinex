import 'package:flutter/material.dart';

class FiatWalletScreen extends StatelessWidget {
  const FiatWalletScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}